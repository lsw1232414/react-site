import { Link, Outlet } from "react-router-dom";

function Layout() {
  return (
    <div>

      <nav>
        <ul>

        </ul>
      </nav>
      <Outlet></Outlet>
    </div>
  );
}
export default Layout;
